runtime: nodejs20

instance_class: F1

env_variables:
  NODE_ENV: production
  EMAIL_USER: songdh418@gmail.com
  EMAIL_PASS: ubggrywvrykvyybl

handlers:
  - url: /.*
    script: auto
    secure: always

automatic_scaling:
  min_instances: 0
  max_instances: 1
  target_cpu_utilization: 0.65
